# SYS-0002 — pickiti-launcher-protocol align (Taskwise docs + contract check)

- **Date:** 2026-09-24 (Asia/Colombo)
- **Risk:** LOW
- **Package:** SYS-0002-D (docs/contracts)
- **Branch:** `syswise/SYS-0002/docs` (includes `syswise/m2-brain-map` @ 07d115f)

## Summary

Deepened Taskwise-side `check-launcher-protocol-contract.mjs` to enforce protocol **1.0.0**
(name/version, source enum, allowlist hosts, storage keys, return paths, consumers).
Docs-only correction of stale SSO claim (`/sso?token=<syswiseJWT>`) to match App Brain /
contract truth: direct `/taskwise/` launch; no live Syswise JWT SSO; return-nav via
`pickiti-launcher-protocol` 1.0.0.

## Files (this package)

- `.syswise/scripts/check-launcher-protocol-contract.mjs`
- `TASKWISE_DOCUMENTATION.md`
- `PICKITI_INTEGRATION.md`
- `.syswise/change-history/SYS-0002.md`

## Out of scope (explicit)

- No JWT/SSO endpoint implementation
- No portal repo edits
- No `.env*`, `deploy.sh`, prisma migrations, merge, PR, or deploy

## Acceptance (docs package)

- [x] Deepened contract check script passes from worktree root
- [x] Stale SSO claim corrected in Taskwise docs
- [x] No JWT/entitlements/SSO endpoint or deploy/merge changes

## Notes for QA / Review

- Portal-side AppsContent `launcher_origin` parity is a separate package via Portal Manager.
- Independent QA should re-run `node .syswise/scripts/check-launcher-protocol-contract.mjs`.
