# SYS-0003 — Task.blockedReason (M3 DB safety)

- **Date:** 2026-09-24 (Asia/Colombo)
- **Risk:** medium / schema_migration
- **db_impact:** migration
- **End state:** Integration branch + PR (no merge, no prod migrate, no deploy)

## Summary

Add nullable `Task.blockedReason String?` so BLOCKED tasks can store an
optional human-readable reason. Forward migration is additive
(`ADD COLUMN IF NOT EXISTS`); rollback drops the column
(`DROP COLUMN IF EXISTS`). Dry-run (migrate deploy → verify → rollback →
re-apply forward) ran on **taskwise_test** only via peer auth / unix socket;
never pointed at production. Database → QA → Review → integration PR only.

## Commits

- `7d8321f` — feat(db): add nullable Task.blockedReason for SYS-0003 M3 safety
- Integration merge on `syswise/SYS-0003/integration`

## Packages (Taskwise)

| ID | Role | Result |
|----|------|--------|
| SYS-0003-D | database | SUCCESS @ 7d8321f (db_impact=migration) |
| SYS-0003-Q | qa | SUCCESS / PASS (independent migrate+rollback on taskwise_test) |
| SYS-0003-R | review | SUCCESS / APPROVE |
| SYS-0003-A | app_manager | integration + PR (this closeout) |

## Migration artifacts

| Artifact | Path |
|----------|------|
| Forward SQL | `server/prisma/migrations/20260924114109_add_task_blocked_reason/migration.sql` |
| Rollback SQL | `server/prisma/migrations/20260924114109_add_task_blocked_reason/rollback.sql` |
| Rollback (scripts copy) | `.syswise/scripts/rollback-SYS-0003-add-task-blocked-reason.sql` |
| Dry-run log (D) | `.syswise/scripts/SYS-0003-D-dry-run.log` |

### Forward

```sql
ALTER TABLE "Task" ADD COLUMN IF NOT EXISTS "blockedReason" TEXT;
```

### Rollback

```sql
ALTER TABLE "Task" DROP COLUMN IF EXISTS "blockedReason";
```

## Schema / brain updates

- `server/prisma/schema.prisma` — `blockedReason String?` on model `Task` (near status / BLOCKED comments)
- `.syswise/database-schema.json` — `Task.fields` includes `blockedReason` (`String?`)

## Dry-run (test DB only)

- Database: `taskwise_test@localhost` (peer auth as `box`, unix socket)
- Steps: `prisma migrate deploy` → verify column → apply rollback SQL → verify gone → re-apply forward so worktree DB matches committed migration
- Evidence: `.syswise/scripts/SYS-0003-D-dry-run.log` (+ QA independent reproduce)

## Acceptance

- [x] Additive reversible Prisma migration only (nullable Task.blockedReason)
- [x] Rollback path documented and dry-run verified on taskwise_test (not prod)
- [x] Independent QA verifies migrate+rollback PASS
- [x] Review APPROVE; integration branch + Taskwise PR only
- [x] No deploy.sh, no prod migrate, no merge, no JWT/entitlements

## Out of scope / bans

- No prod migrate, no `deploy.sh`, no `.env*` writes
- No JWT / entitlements / SSO changes
- No FE/BE business logic beyond schema
- No merge to main / no production deploy
